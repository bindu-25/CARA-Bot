version: '3.8'

services:
  cara-bot:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8501:8501"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ../data/uploads:/app/data/uploads
      - ../logs:/app/logs
